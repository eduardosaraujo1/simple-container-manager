#ifndef CONTAINERPREFERENCES_H
#define CONTAINERPREFERENCES_H

/*
 * Needs to specify which label to use
 * Needs to specify which icon to use (path to icon files if Qt Widgets uses that for images)
 * Needs to specify which command to run
 */
struct ContainerPreferences
{
};

#endif // CONTAINERPREFERENCES_H
